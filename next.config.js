/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "sinim-api-git-tools-prodominicanadev.vercel.app",
      "images.unsplash.com",
    ],
  },
};

module.exports = nextConfig;
